# V12.0.1 台股即時價解析修正

- `price` 優先採用 TWSE MIS 的 `z`（最新成交價）。
- 不再把 `pz` 無條件當作即時價；僅在落於今日高低區間且不等於昨收時採用。
- `z` 缺值時，依序使用有效的買賣中間價、最佳買價、最佳賣價，最後才退回昨收。
- API 新增 `quoteType`、`bestBid`、`bestAsk`，方便辨識報價性質。
- 更新版本及 PWA 快取為 V12.0.1。
